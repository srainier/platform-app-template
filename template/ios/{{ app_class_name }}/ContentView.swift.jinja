import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack {
            Text("{{ app_display_name }}")
                .font(.title)
            Text("Start editing ContentView.swift to build your iOS app.")
                .foregroundColor(.secondary)
        }
        .padding()
    }
}

#Preview {
    ContentView()
}
